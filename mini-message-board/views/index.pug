extends layout

block content
  h1= title
  p Welcome to #{title}

  ul
  each message in messages
    li= message['text']
    li= message['user']
    li= message['added']
  
  a(href="/new") Add a new message 